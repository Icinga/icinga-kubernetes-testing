apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: tests.icinga.com
spec:
  group: icinga.com
  scope: Namespaced
  names:
    kind: Test
    listKind: TestList
    plural: tests
    singular: test
    shortNames:
      - tst
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: { }
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                deploymentName:
                  type: string
                tests:
                  type: array
                  items:
                    type: object
                    properties:
                      testKind:
                        type: string
                      goodReplicas:
                        format: int32
                        type: integer
                        minimum: 1
                        maximum: 10
                      badReplicas:
                        format: int32
                        type: integer
                        minimum: 0
                        maximum: 10
                      testConfig:
                        type: string
                    required:
                      - testKind
                      - goodReplicas
                      - badReplicas
                      - testConfig
            status:
              type: object
              properties:
                availableReplicas:
                  format: int32
                  type: integer
                  minimum: 0
